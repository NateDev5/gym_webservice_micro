spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:trainer_db}
    username: ${POSTGRES_USER:gym}
    password: ${POSTGRES_PASSWORD:gym}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
