spring:
  datasource:
    url: jdbc:h2:mem:member-service-test-db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: false
